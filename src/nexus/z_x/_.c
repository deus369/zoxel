#if !defined(zoxm_game) // && defined(zoxm_players)
#define zoxm_game

zox_begin_module(ZoxGame)
    game_name = "Z:x";
    // app settings
    headless = 1;
    nosounds = 1;
zox_end_module(ZoxGame)

#endif