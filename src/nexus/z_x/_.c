#if !defined(zox_mod_game) // && defined(zox_mod_players)
#define zox_mod_game

zox_begin_module(ZoxGame)
    game_name = "Z:x";
    // app settings
    headless = 1;
    nosounds = 1;
zox_end_module(ZoxGame)

#endif