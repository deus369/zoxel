// todo: convert camera mouse point to a ray origin and normal
//      > we can use frustum actually, the near and far plane, since we have its corners
//      > camera_point_to_ray

Ray camera_point_to_ray() {
    Ray ray = { };

    return ray;
}
